# ── Base de datos (RDS del equipo)
DATABASE_URL=postgresql+asyncpg://cropsight:devbackend@cropsight-dev.chumg2aaac61.us-east-2.rds.amazonaws.com:5432/cropsight

# ── JWT
SECRET_KEY=desarrollo_local_key_32_caracteres_ok
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── AWS (mock temporal)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=fake
AWS_SECRET_ACCESS_KEY=fake
S3_BUCKET_IMAGES=cropsight-images
S3_BUCKET_MODELS=cropsight-models

# ── Inferencia (mock/local)
LAMBDA_INFERENCE_URL=http://localhost:9000/inference

# ── MLflow (ignorar por ahora)
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_MODEL_NAME=cropsight-classifier

# ── GitHub Actions (mock)
GITHUB_TOKEN=fake
GITHUB_REPO=org/cropsight
GITHUB_WORKFLOW_ID=retraining.yml

# ── Entorno
ENVIRONMENT=development